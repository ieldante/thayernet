# Figure Captions

## `direct_unet_success.png`

Example successful reconstruction. The direct U-Net removes a visually significant contaminant while preserving the target galaxy structure.

## `direct_unet_partial_failure.png`

Partial failure case. The model suppresses some contaminating structure but loses target detail in an ambiguous overlapping region.
