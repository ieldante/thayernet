# Scientific D3 result

The append-only Thayer-D3S record is
`outputs/runs/thayer_scientific_d3_20260713_170508/`.

Status: **EXECUTABLE BUNDLE REGRESSION — D3 NOT RUN**.

The required bundle SHA-256 matched and the canonical registry contained 180
entries. A standard-library-only preregistration-completeness audit then found
that the bundle does not define the required expert-activity/death gate,
prompt-collapse stop, optional tangent protocol, six-category outcome mapping,
or complete semantic-state rules. The campaign was required to obtain each
setting from the bundle and was forbidden to infer or repair one, so it stopped
before preregistration, third-party imports, or scientific tensor loading.

D0/D1/D2 were not rerun. No decoder was instantiated, no gradient or optimizer
step occurred, and no D3 trajectory exists. L0 capacity remains scientifically
unknown. Eight-scene fitting and the decoder-capacity ladder remain
unauthorized. Ordinary and broader scenes, Atlas, development, and lockbox were
untouched; all 600 historical checkpoints remained unchanged.

Exactly one next experiment is authorized: a metadata-only executable-contract
v3 campaign that freezes the missing definitions, adds fail-closed corruption
tests for each, and emits a new hashed bundle without loading scientific
tensors or running D3.

## Policy-contract follow-up

Thayer-D3P completed that metadata-only correction. Sixteen executable policies,
76 fixtures, 106 branches, 256 outcome combinations, 11 semantic states, and
30 corruption tests passed. The launcher emitted
`READY_FOR_SCIENTIFIC_D3_EXECUTION` for bundle-v3 SHA-256
`30ac88c635774d0fb4518bedde66fa459d67b1c1a323816c12d1e37b4614b61c`.

Scientific D3 still has not run. No scientific tensor, model, optimizer,
decoder forward, or D3 step was used, so L0 success and failure remain unknown.

## Thayer-D3I result

V4 integration passed and authoritative continuation loaded eight exact
containers / 91 counted members. It stopped on a dtype-token validation defect
and blocked runtime-cache reads before model or optimizer construction.
Outcome: `IMPLEMENTATION_OR_CONTRACT_FAILURE`; authorization: `none`.
Scientific D3 coverage and L0 sufficiency remain unknown.

## V4.1 retry result

The two authorized v4 corrections passed, as did validation of eight
containers, 91 members, and four D1 dtype contracts. A separate premodel
inventory-hash implementation error then stopped the frozen candidate. Model,
optimizer, forward, and step counts remain zero. Outcome is again
`IMPLEMENTATION_OR_CONTRACT_FAILURE`; authorization remains `none`.

R1 did not reach scientific metadata or payload access. Its terminal event was
an orchestrator log collision before candidate-002 worker launch. Model,
optimizer, forward, and step counts remain zero; L0 sufficiency is unknown.
