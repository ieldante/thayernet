# Decoder capacity ladder

Thayer-CL is the fail-closed contract preflight in
`outputs/runs/thayer_capacity_ladder_20260713_013132/`. Its preregistration
SHA-256 is
`d44778017b45a1a21109b19cd5e623c76b0a2353128c10a5ad8a7edbfb27820c`
and predates every campaign per-scene load, model construction, and optimizer
step.

All 24 required Thayer-FP projection and neural checks reproduced. The P0
targets remained fully feasible, while the diagnostic neural result retained
zero scientific coverage, ordinary expert diameter 3.564, prompt swap 0.984,
forward consistency 0.969/1.000, and final negative fraction 43.57% in the
historical normalized-output convention.

The P0 representatives were nearly exact truth: their median truth-homotopy
alpha was 0.999979 and median normalized correction was 0.946369. Had the
output-contract prerequisite passed, shared encoder, depth, kernels, skips,
activation and normalization families, assignment, targets, optimizer, and
budget would have remained fixed; decoder width was the only prospective
ladder variable.

The decoder ladder was not run. The inherited linear head is the only output
mapping defined by the frozen neural contracts, and it permits negative
physical source contributions. ReLU, square, and absolute-value heads are
distinct new mappings that can represent the nonnegative projected-target
range, but no frozen contract selects among them. Choosing one in Thayer-CL
would violate the preregistered uniqueness gate.

No L0-L3 model was constructed, no one-scene or eight-scene fit occurred, and
no capacity threshold was estimated. Decoder capacity therefore remains
unresolved. The one next experiment is a separately preregistered
output-parameterization campaign at fixed L0 capacity, before any width ladder.

Strict correctness is `PASS`: all 27 checks passed, the pre-load inventory
captured all 594 historical checkpoints, and the closure audit found all 594
unchanged. The earlier append-only `20260713_005215` attempt remains preserved
as a superseded strict-correctness failure because its initial inventory
captured only 59/594 checkpoints before per-scene loading.

This was a training-only 64-row diagnosis. Even a future microset-memorization
pass would not establish generalization. Atlas, development, and lockbox data
were untouched.

## Thayer-OP follow-up

Thayer-OP held L0 capacity fixed and changed only the in-forward physical
mapping. ReLU, square, and absolute value all passed target representability,
stop-rule self-tests, and synthetic fitting with nonnegative outputs. None
passed the frozen final ordinary or ambiguous one-scene truth-coverage gate.
The eight-scene stage was therefore not run, no mapping was frozen, and the
decoder-capacity ladder remains unauthorized. Thayer-CL measured no capacity
result, and Thayer-OP did not compare capacities.

## Fixed-feature reachability result

The repository-integrity retry produced a scientific result without changing
capacity. Square passed D0 and D1, but its 204-parameter final-head-only D2
condition failed own, alternate, and both-mode coverage. The frozen progression
rule therefore blocked D3 and all tangent diagnostics. This is classified as a
frozen-feature conditioning barrier, not a decoder-capacity barrier. L1-L3
construction and any broader capacity ladder remain unauthorized.

## Thayer-D3 disposition

Thayer-D3 did not test decoder capacity. Its persisted D0-D2 controls and square
initial state reproduced, but the authoritative D1 output artifact omitted the
optimized penultimate tensors required by the prospective feature-trajectory
contract. The campaign stopped before optimizer construction and produced no
D3 gradient, trajectory, tangent, or capacity evidence.

The decoder-capacity ladder remains unauthorized. The one next experiment is a
square-only D1 endpoint-persistence replay with unchanged capacity; it must not
construct L1-L3 or open broader data.

## Thayer-D3R disposition

The D1 endpoint prerequisite is now complete, but the authoritative retry
stopped during guarded runtime bootstrap before any decoder gradient or update.
It therefore supplies no evidence for L0 sufficiency or insufficiency. L1-L3
construction and the decoder-capacity ladder remain unauthorized.

## Thayer-D3A disposition

D3A stopped even earlier than decoder optimization: preregistration could not
freeze the scientific sky and plausibility-threshold inputs for the forward
gate from the isolated allowlisted evidence. No decoder was constructed and no
capacity evidence was produced. L1-L3 construction and the decoder-capacity
ladder remain unauthorized.

## Thayer-D3E executable-contract follow-up

Thayer-D3E resolved capsule-consumer drift and proved that the exact 46,470
parameter-per-expert L0 architecture and frozen optimizer path execute on
production-shape synthetic tensors. This is not a capacity comparison and does
not change the prior D2 or ladder decisions. No scientific D3 step, L1-L3
construction, broader-scene fit, Atlas access, development access, or lockbox
access occurred. The next campaign is the already authorized one-scene L0 D3,
not a capacity ladder.

## Thayer-D3S decision

The one-scene L0 scientific run did not occur. A required expert-activity/death
definition and related trajectory settings are absent from the frozen bundle,
so D3S stopped fail-closed before science. This is not capacity evidence. The
decoder-capacity ladder remains unauthorized.

## Thayer-D3P disposition

Policy closure is not decoder-capacity evidence. The new authorization engine
allows a capacity ladder only after an authoritative D3 failure classified
`DECODER_PARAMETERIZATION_CAPACITY_BARRIER`, with authoritative D0/D1 passes,
no code/runtime/loss/mapping/assignment defect, and valid tangent evidence if
tangent evidence is used. No scientific D3 trajectory exists yet, so the
ladder remains unauthorized.

## Thayer-D3I authorization

The frozen outcome was `IMPLEMENTATION_OR_CONTRACT_FAILURE`, so the exact v3
authorization engine returned `none`. No scientific D3 trajectory or clean
decoder failure exists. A decoder-capacity ladder remains unauthorized.

## V4.1 authorization

The retry again produced no scientific D3 trajectory. Candidate 002 stopped
before model construction on a member-inventory implementation error, so the
unchanged authorization engine returned `none`. The capacity ladder remains
unauthorized.

R1 did not produce an independently eligible candidate or any D3 trajectory.
The capacity ladder remains unauthorized.
