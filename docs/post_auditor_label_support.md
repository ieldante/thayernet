# POST-auditor label support

Condition C supplies a verified strong unsafe class but no safe class: the
unchanged replay returned 12,493 unsafe and zero safe POST episodes. Episode
construction and labeling remain correct.

Thayer-PU Eligibility v1 stopped at deployment preflight, before any full
output freeze or safety labeling. Consequently safe/unsafe support for
Thayer-PU is unknown, the frozen minimum support gates were not evaluated, and
Condition C plus Thayer-PU cannot authorize binary POST-auditor training,
validation, calibration, or leave-one-family-out evaluation. No auditor was
trained.

The next authorized work is not an auditor fit. It is the single separately
preregistered Family-D frozen-family eligibility audit named in
`docs/thayer_pu_eligibility_v1.md`.

## Family-E v0 update

Thayer-PU Batch-R1 supplied 0 safe / 7,591 unsafe outputs. Family-E v0 supplied
no outputs because its preregistered simplex physical parameterization could
not represent the signed zero-background observations. POST binary learning
remains impossible. No auditor was trained.

## Signed-noise-residual preflight update

The corrected Family-E1 physical contract passed but produced no reconstruction
labels. POST support remains Condition C 0/12,493 safe/unsafe and repaired
Thayer-PU 0/7,591. No auditor is authorized until the separate Family-E1 model
campaign supplies actual source-group-safe OOF outputs with both classes.

## Family-E1 v0

Family-E1 v0 added no POST labels. A preregistered reconstruction micro gate
failed before checkpoint freeze, so safe/unsafe support is unmeasured. The
future auditor may not use micro outputs or substitute in-sample predictions;
genuine source-group-safe OOF Family-E1 outputs do not exist from this run.
