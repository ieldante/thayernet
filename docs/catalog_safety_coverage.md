# Catalog safety and coverage

Status: **NOT EVALUATED** for competing-hypothesis auditing.

The intended protocol reports catastrophic false-safe rate, source-confusion
acceptance, null-hallucination acceptance, ambiguous forced-output acceptance,
Atlas-witness acceptance, safe-output rejection, and accepted-catalog flux,
color, centroid, and valid shape bias at 95%, 90%, 80%, 70%, and 50% accepted
coverage. Intervals must cluster by source duplicate group, results must be
shown for every held-out family and their macro-average, and random rejection
plus an oracle ranking bound must be included.

The current campaign did not train a cross-family auditor or freeze a catalog
admission rule, because fewer than three meaningfully distinct compatible
families existed. Therefore no false-safe-versus-coverage curve, accepted-
catalog bias estimate, safe-rejection rate, or final-survey claim exists. Atlas
failure counts must not be relabeled as catalog-policy performance.

Atlas v0 leaves this status unchanged. Because the ambiguity-evidence baseline
gate failed, no coverage threshold, false-safe curve, accepted-catalog bias, or
safe-output rejection estimate was computed.
