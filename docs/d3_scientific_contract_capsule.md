# D3 Scientific Contract Capsule

Thayer-D3C completed the metadata-only scientific contract capsule under
`outputs/runs/thayer_d3_scientific_capsule_20260713_155637/`.

Status: **SCIENTIFIC CONTRACT CAPSULE PASS**.

The capsule embeds every small value needed to construct the D3 forward and
truth-coverage gates: band and channel order, physical units, normalization,
the exact g/r/z sky-electron vector, all forward-plausibility thresholds, all
truth-coverage thresholds, numerical tolerances, prompt and assignment rules,
and implementation hashes. The capsule SHA-256 is
`8a76ccdfa659a7291f0f9b73e0cb4d4c8adfb317b9902fc8ad5763e6d17b7d21`.

Large scientific tensors are not copied into the capsule. The cached features,
one-scene P0 payload, D1 endpoint, and initial square-decoder state are bound by
exact relative path, byte size, expected member names, schema, and SHA-256.
Thayer-D3C deserialized none of them.

The prior authoritative D3 campaign produced no scientific result. Runtime
readiness had already passed; its remaining blocker was an incomplete
scientific contract. Thayer-D3C resolves that contract blocker but does not run
D3. Authoritative D3 is now contractually eligible for one new, separately
preregistered capsule-only campaign. D3 remains scientifically unknown until
that campaign runs.

## Thayer-D3E executable-contract resolution

The downstream capsule-driven attempt confirmed that capsule v1 was
scientifically complete but consumer-incomplete: its self-validator passed,
while the actual D3 consumer required nine additional evidence, architecture,
initialization, and tensor-member declarations. Thayer-D3E preserved capsule v1
and built append-only capsule v2 from one shared 180-requirement registry.

Capsule v2, its builder, validator, preflight, and consumer now have identical
required sets. Exact architecture and optimizer construction, one synthetic MPS
step, checkpoint replay, and all 25 fail-closed corruptions passed. No
scientific value or D3 step was used. One new campaign may freeze executable
bundle hash `884d35e7ee385b2bb0b7d0e65aaf6bc18121fa209db6a17cc101ef12e32f4045`;
D3 remains scientifically unknown.

## Thayer-D3S limitation

Capsule v2 closes the requirements enumerated by its own 180-entry registry,
but the downstream D3S request requires additional executable stop and outcome
definitions. The missing expert-activity/death gate alone makes the scientific
preregistration incomplete. No capsule value was reinterpreted or extended;
D3S stopped before scientific access.

## Thayer-D3P control-policy completion

The capsule and its scientific thresholds remain unchanged. Thayer-D3P added a
separate executable policy registry and bundle v3, then covered every launcher
policy branch with synthetic scalar/event fixtures. This policy pass used no
capsule scientific tensor and makes no new scientific claim.

## Thayer-D3I preservation

Capsule v1 remained byte-identical and supplied the scientific authority
reference through bridge v4. All referenced container hashes matched. The
scientific worker stopped on its own dtype-token comparison before using the
capsule evaluator, so no capsule threshold or scientific value changed and no
new D3 claim was produced.
