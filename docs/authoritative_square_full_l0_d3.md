# Authoritative Square-Only Full-L0 D3

## Thayer-D3A disposition

The authoritative append-only record is
`outputs/runs/thayer_authoritative_d3_20260713_145040/`. Its standard-library
bootstrap matched all 27 frozen runtime-readiness hashes, all 11 named
scientific-container hashes, and all 600 historical checkpoint hashes before
any third-party import or tensor deserialization. The controlling
preregistration SHA-256 is
`209aa4a2fdad6917536a010cb497eeb437c5b1a0994f948d279b60b63972e899`.

The campaign then stopped at its required-setting completeness gate. The pure
forward evaluator requires an explicit scientific sky-electron vector and
global, per-band, and relative-flux plausibility thresholds. Runtime readiness
persisted the evaluator source hash and synthetic reference tests, but its
frozen manifests do not contain the scientific values or an isolated
non-Atlas artifact containing them. The D1R manifest persists successful
metrics, not those evaluator inputs.

The synthetic readiness fixtures are not substitutes for scientific values,
and the historical source paths are prohibited in this campaign. No value was
inferred, copied from a protected partition, or invented. The scientific
interpreter was not launched; no cached feature, target, endpoint, model,
optimizer, gradient, decoder forward, checkpoint, replay, or postprocessing
result exists.

Status: **PREREGISTRATION INCOMPLETE — D3 NOT RUN**. This is not evidence for
decoder optimization, decoder capacity, hard assignment, or square-map
conditioning. Neither square-only eight-scene work nor a decoder-capacity
ladder is authorized.

Exactly one next experiment is justified: a metadata-only forward-gate
contract-isolation audit that persists the exact scientific sky vector and
plausibility thresholds, with hashes and provenance, into a non-Atlas artifact
without loading a scene tensor or running D3.
