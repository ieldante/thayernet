# D3 v4.1 scientific execution

Candidate 002 became authoritative after 58 focused/inherited tests, one
unchanged platform skip, the actual synthetic full stack, exact checkpoint
replay, isolated postprocessing, closed argument/requirement flow, 25/25
inherited corruptions, 18/18 v4.1 corruptions, an explicit MPS backward and
optimizer step, 13/13 frozen-v4 source hashes, and 600/600 historical
checkpoint hashes passed.

Mandatory continuation then loaded the same eight allowlisted containers and
counted the same 91 members. Serialization prewarm passed, strict
serialization `.pyc` reads remained zero, and all four D1 dtype contracts
passed canonical comparison. Execution stopped before model construction when
the added complete-member inventory attempted to apply the frozen CHW-only
canonical hash to a higher-rank cached-feature tensor:
`CHW requires one rank-3 sample and no sample_index`.

The source-frozen candidate was not patched or retried. Model constructions,
optimizer constructions, decoder forwards, and optimizer steps are all zero.
D0/D1/D2 metadata were restated from the frozen bundle without tensor reload;
no D3 trajectory exists. The exact policy outcome is
`IMPLEMENTATION_OR_CONTRACT_FAILURE`, downstream authorization is `none`, and
L0 capacity remains scientifically unknown.

Exactly one next experiment is specified: an append-only v4.2
member-inventory hash-domain correction, preregistered to use a rank-aware
inventory hash without changing scientific/model/policy values, followed by
the same one-scene D3 retry only after all v4.1 gates re-pass.

## R1 superseding status

The independent R1 campaign stopped before eligibility because candidate 002
reused candidate 001's synthetic log path. No metadata or payload access
occurred, and the CHW defect was neither reached nor repaired.
