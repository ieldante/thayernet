# D3 v4.1 R1 scientific execution

Scientific execution was not authorized. Candidate 002 encountered an
unrelated append-only orchestrator log collision before its synthetic worker
could launch, so the independent validator did not emit an eligibility marker
and no source-freeze authorization existed.

Scientific containers, members, models, optimizers, decoder forwards, and
optimizer steps all remain at zero for this run. The downstream CHW issue did
not recur because the campaign stopped earlier. L0 capacity remains unknown;
eight-scene work and the capacity ladder remain unauthorized.
