# D3 Control-Policy Contract

The append-only Thayer-D3P campaign is recorded at
`outputs/runs/thayer_d3_policy_contract_20260713_173955/`.

Status: **D3 POLICY CONTRACT PASS — SCIENTIFIC D3 NOT RUN**.

Executable bundle v2 passed its own 180-requirement contract but omitted five
control-policy families required by the scientific launcher. Thayer-D3P froze
those families prospectively before fixtures or launcher changes and expanded
the complete executable control surface to 16 canonical policies.

The pure engine passed 76 deterministic scalar/event fixtures and executed all
106 declared branches. All 256 outcome-evidence vectors mapped to exactly one
of nine categories. All 11 semantic states passed append-only persistence,
collision refusal, deterministic selection, not-reached handling, and fresh-
process replay. All 30 bundle-v3 corruptions were rejected with the expected
policy identifier.

The actual scientific launcher delegates its policy-preflight mode to the pure
engine and emitted, in order:

1. `ALL_D3_POLICIES_EXECUTABLY_DEFINED`
2. `ALL_D3_CONTROL_BRANCHES_SYNTHETICALLY_COVERED`
3. `DECLARED_DEFINED_ACCESSED_TESTED_PERSISTED_POLICIES_EQUAL`
4. `READY_FOR_SCIENTIFIC_D3_EXECUTION`

The frozen executable bundle-v3 SHA-256 is
`30ac88c635774d0fb4518bedde66fa459d67b1c1a323816c12d1e37b4614b61c`.
No scientific tensor, model, optimizer, decoder forward, or D3 step was used.
Atlas, development, and lockbox were untouched. One separately preregistered
scientific D3 campaign may freeze this exact hash and proceed; this policy
campaign does not continue into D3.

## Thayer-D3I integration closure

V4 propagated bundle v3 through the actual scientific worker and passed the
full synthetic/negative/flow gates. The frozen scientific invocation later
encountered an artifact-token mismatch plus two blocked runtime-cache reads
before model construction. Stop precedence selected `ACCESS_GUARD_VIOLATION`,
the outcome engine assigned `IMPLEMENTATION_OR_CONTRACT_FAILURE`, and the
authorization engine returned `none`.

## V4.1 policy closure

No bundle-defined scientific trajectory event was reached. The exact outcome
engine mapped the new premodel inventory implementation failure to
`IMPLEMENTATION_OR_CONTRACT_FAILURE`, and the unchanged authorization engine
returned `none`. Terminal-failure and final semantic states were persisted;
nine other states were explicitly not reached and replay passed.

R1 stopped before a scientific policy state existed. No outcome evidence about
optimization, assignment, square mapping, or capacity was generated; the
campaign-level result is implementation failure with authorization `none`.
