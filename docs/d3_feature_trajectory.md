# D3 Feature Trajectory

## Status

No D3 feature trajectory exists. Thayer-D3 stopped before the one-step trace
and before optimizer construction because the authoritative successful D1
artifact did not persist its free penultimate tensors.

The initial square L0 penultimate tensors reproduced byte-for-byte from the
immutable joined prompt-A/B cache. Prompt A and prompt B remained distinct at
all three cached feature levels, every cached tensor was finite, and the cache
hashes were unchanged. The initial expert diameter was 3.9034, set prompt-swap
was 1.0, and the persisted forward-consistency result was 1.0.

No L2 distance, cosine similarity, channel or spatial correlation,
singular-spectrum difference, induced-output distance, displacement-recovery
fraction, z-band-versus-feature curve, or target-direction capture is reported.
Computing those quantities without the actual D1 penultimate endpoint would
create a false reference. D1 remains evidence that at least one feasible
feature configuration existed; it is not treated as unique.

The next evidence step is the separate D1 endpoint-persistence replay described
in the full D3 diagnostic. D3 must remain closed until that reference is saved
and independently mapped through the frozen heads.
