# D3 Executable Bundle v3

Bundle v3 is append-only and preserves bundle v2 unchanged. Its canonical path
is
`outputs/runs/thayer_d3_policy_contract_20260713_173955/bundle_v3/d3_executable_bundle_v3.json`,
and its SHA-256 is
`30ac88c635774d0fb4518bedde66fa459d67b1c1a323816c12d1e37b4614b61c`.

The bundle references the complete v2 scientific and architecture contract;
the 16-policy registry and schema; hashed policy engine and launcher; branch
manifest and executed coverage; set-equality proof; exhaustive outcome table;
semantic-state schema and persistence tests; tangent audit; stop precedence;
and downstream authorization table.

Thirty corrupted bundle or registry variants were rejected before scientific
work. The actual launcher validated the exact bundle, re-executed every
synthetic fixture and state branch, emitted all four readiness markers, and
exited. No scientific tensor, model, optimizer, decoder forward, or D3 step was
used. The next scientific campaign must freeze this exact hash.

## Thayer-D3I consumer result

Bundle v3 propagated end to end through bridge v4. The actual v4 synthetic
path passed, and 25 integration corruptions were rejected. Scientific
continuation loaded eight contract-referenced containers, then stopped before
model construction on `D3I-D1-MEMBER-CONTRACT`; bundle v3 itself did not fail.

## V4.1 propagation

Candidate 002 preserved bundle-v3, base-v2, capsule-v1, runtime, policy, model,
optimizer, and execution-budget hashes. Bundle v3 propagated through synthetic
and authoritative workers. The authoritative stop followed successful dtype
validation and arose only in added member-inventory reporting, before bundle
scientific controls or model construction.

R1 preserved bundle-v3, bundle-v2, capsule, model, optimizer, threshold, and
budget hashes. Candidate 002 was never independently eligible because an
unrelated append-only log collision stopped orchestration before worker launch.
